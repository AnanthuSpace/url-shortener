export interface ShortenedUrl {
    shortLink: string
    originalLink: string
    createdAt: string
  }
  
  export interface fetchUrl {
    shortUrl: string,
    longUrl: string,
    createdAt: string
  }
  